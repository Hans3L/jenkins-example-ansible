pipeline {
  agent {label 'linux'}

  stages {
    stage('Hello') {
      steps {
        sh '''
          sudo ansible --version
          ansible-playbook --version
          ansible-galaxy --version
        '''
      }
    }
  }
}
