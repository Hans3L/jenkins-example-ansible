pipeline {
  agent { label 'linux' }
  environment {
   ANSIBLE_PRIVATE_KEY=credentials('carvajaldev-private-key') 
  }
  stages {
    stage('Hello') {
      steps {
        //sh 'ansible-galaxy collection install -r requirements.yml'
        sh 'ansible-playbook -i inventory/dev.hosts --private-key=$ANSIBLE_PRIVATE_KEY playbooks/message.yml'
      }
    }
  }
}
